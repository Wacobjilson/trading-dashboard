# Normalize line endings to LF in the repo (avoids CRLF breaking Docker builds,
# shell scripts, and Go/Node tooling on Linux CI).
* text=auto eol=lf

# Treat these as binary (no EOL conversion)
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
